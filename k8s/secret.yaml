apiVersion: v1
kind: Secret
metadata:
  name: travel-ai-secrets
  labels:
    app: travel-ai
type: Opaque
stringData:
  # ⚠️  IMPORTANT: Replace these with real values before deploying!
  # In production, use a secrets manager (Vault, AWS Secrets Manager, etc.)
  google-api-key: "REPLACE_WITH_YOUR_GOOGLE_API_KEY"
  jwt-secret: "REPLACE_WITH_A_STRONG_RANDOM_SECRET"
  mongo-url: "mongodb://mongo:27017/travel_agent_db"
